{
  "dependencies": {
    "dotenv": "^16.3.1",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.6.1"
  },
  "scripts": {
    "build-deploy": " cp -r src/. dist/ && cp -r node_modules/. dist/node_modules/ && esbuild dist/index.ts --bundle --minify --sourcemap --platform=node --target=es2020 --outfile=dist/index.js",
    "build": "tsc",
    "start": "npm run build && sam local invoke validateCustomer -e event.json"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.4",
    "@types/node": "^20.8.7",
    "typescript": "^5.2.2"
  }
}
